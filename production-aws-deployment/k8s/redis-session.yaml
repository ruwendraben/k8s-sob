apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-session
  namespace: sob
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-session
  template:
    metadata:
      labels:
        app: redis-session
    spec:
      containers:
        - name: redis
          image: redis:7.2
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis-session
  namespace: sob
spec:
  selector:
    app: redis-session
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP
