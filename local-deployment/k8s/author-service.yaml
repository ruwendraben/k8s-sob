apiVersion: v1
kind: Service
metadata:
  name: sob-author
  namespace: sob
spec:
  selector:
    app: sob-author
  type: NodePort
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      nodePort: 30081
